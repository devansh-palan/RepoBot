[pytest]
# Only our tests. data/repos/ holds cloned repositories for the eval, and they
# ship their own test suites — without this pytest collects those too and the
# run drowns in someone else's failures.
testpaths = tests
norecursedirs = data .venv .git __pycache__
